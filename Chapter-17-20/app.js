// Q-1 Declare and initialize an empty multidimensional array.
// (Array of arrays)

function ans1() {

let multiArray = [];

multiArray.push([]);
multiArray.push([]);
multiArray.push([]);

console.log(multiArray);
}